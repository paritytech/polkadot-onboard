{
  "name": "@polkadot-wallets/react",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf ./dist/ && npm run format && tsc --build",
    "build:watch": "tsc --watch",
    "format": "prettier --write \"./src/**/*.{ts,tsx}\""
  },
  "dependencies": {
    "@polkadot-wallets/core": "^1.0.0",
    "@polkadot/extension-inject": "^0.44.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
