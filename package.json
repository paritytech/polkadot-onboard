{
  "name": "polkadot-wallets",
  "version": "1.0.0",
  "repository": "https://github.com/SaltyCucumber/polkadot-wallets.git",
  "author": "saltycucumber",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build:core": "cd packages/core && yarn build",
    "build:injected-wallets": "cd packages/injected-wallets && yarn build",
    "build:wallet-connect": "cd packages/wallet-connect && yarn build",
    "build:react": "cd packages/react && yarn build",
    "build": "yarn build:core && yarn build:injected-wallets && yarn build:wallet-connect && yarn build:react",
    "start:react-headless": "yarn build && cd examples/react-headless && yarn start",
    "start:react-next": "yarn build && cd examples/react-next && yarn dev",
    "format": "prettier --write \"./src/**/*.{ts,tsx}\""
  }
}
