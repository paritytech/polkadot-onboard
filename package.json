{
  "name": "dotsama-wallets",
  "version": "1.0.0",
  "repository": "https://github.com/SaltyCucumber/dotsama-wallets.git",
  "author": "saltycucumber",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build:core": "cd packages/core && yarn build",
    "build:injected-wallets": "cd packages/injected-wallets && yarn build",
    "build:wallet-connect": "cd packages/wallet-connect && yarn build",
    "build:react": "cd packages/react && yarn build",
    "start": "yarn build:core && yarn build:injected-wallets && yarn build:wallet-connect && yarn build:react && cd packages/react-demo && yarn start",
    "start:react-dapp": "yarn build:core && yarn build:injected-wallets && yarn build:wallet-connect && yarn build:react && cd examples/react-dapp && yarn dev",
    "build": "yarn build:core && yarn build:injected-wallets && yarn build:wallet-connect && yarn build:react && cd packages/react-demo && yarn build",
    "format": "prettier --write \"./src/**/*.{ts,tsx}\""
  }
}
